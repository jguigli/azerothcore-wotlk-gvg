# Good practice with a fork

Update with main repo :
```
git fetch upstream
git checkout master
git merge upstream/master
git push origin master
```


## Don't forget

Set GameType = 16 in env/dist/etc/worldserver.conf